# 朝のチェックイン 実装計画・完成前整理

更新日: 2026-08-19

## この文書の位置づけ

この文書は当初のPhase 1〜7の実装計画として作成したが、現在はその大半が実装済みである。
古い「追加予定」「次に実装」表現を残すと現在地を誤認するため、完成前の現状整理として更新する。

## 目的

校内LANで運用するWebアプリとして、児童の朝のチェックイン、心の天気、朝の準備タイマー、出席・健康観察、月間・学期・年度集計を一体化する。

忙しい担任が毎朝迷わず使えることを最優先とし、児童側はiPad / Android等のブラウザ利用を基本とする。

## 現在実装済みの主機能

- 児童用の朝チェックイン画面
- 低学年向け表示 / 高学年向け表示
- 学年・組表示
- 現在時刻、朝の会までの残時間、タスクタイマー
- 心の天気4段階
- 朝のチェック項目、自由追加、完了操作
- 正式タスクSVG5種と画像読込失敗時のフォールバック
- 担任ホーム
- 学級・端末・画面初期設定
- C4th CSV名簿取込の受け皿
- 担任用タスク編集・プリセット・学年別プレビュー
- Node.js / Express / WebSocket校内LANサーバー
- 端末ACK、接続状況、再接続、オフライン継続
- 今日の出席・健康観察
- 「未チェックイン＝欠席確定」にしない未確認運用
- 日次確定と監査履歴
- 月間出席簿の横計・縦計・理由別集計
- 学期・年度集計
- 授業日カレンダー、振替休業等の除外
- 学期設定
- 心の天気の週間・月間分析と行事メモ
- Smoke Test / Visual Check / preflight

## 壊さないためのデータ原則

出席・健康観察・心の天気は、同じ担任画面で参照する場合があっても別データとして扱う。

- 心の天気を出欠区分へ自動変換しない
- 心の天気を健康状態へ自動変換しない
- 出席・健康の保存で心の天気を上書きしない
- 未チェックインを即欠席にしない
- 正式な欠席理由・遅刻・早退は担任が確定する

## 端末運用方針

標準はインストール不要のブラウザ運用。

- iPad: Safari等から校内LAN URLを開く
- Android: Chrome等から校内LAN URLを開く
- PWA / Web Clip / 自動起動: 学校MDMで許可される場合のみ
- Webページ単独ではブラウザの指定時刻強制起動はできない
- 一時切断時は直前設定で継続し、復帰後に自動再接続

詳細は `git clone/school-checkin-timer/LAN_SETUP.md` を正とする。

## 現在のアプリ配置について

現ブランチではアプリ本体が次のパスにある。

```text
git clone/school-checkin-timer/
```

このネストは一般的なリポジトリ構成としては不自然だが、現在のGitHub Actions、相対パス、プレビュー、運用文書がこの配置を前提に動いている。

**完成直前にルート移動すると広範囲のパス変更が発生し、正常部分を壊す危険が高い。したがって今回の完成工程では移動しない。**

リポジトリ構成の整理は、現行版を完成・固定した後の独立した保守作業として扱う。実施する場合は別ブランチで全パス・CI・静的資産・起動手順を一括検証する。

## 完成前の自動確認

### `npm run preflight`

- Node.js 20以上
- 主要画面・主要ファイル
- 正式タスクSVG5種
- データ保存ディレクトリ書込み
- npm scripts
- 8080番ポート利用可否

### `npm run test:smoke`

- サーバーAPI
- WebSocket
- 設定同期
- 出席・健康・心の天気の分離
- 学校暦・学期設定
- 主要HTML
- 正式SVG取得

### `npm run test:visual`

35パターンを対象に、1366 / 1024 / 768 / 375pxを中心として以下を確認する。

- 低学年 / 高学年児童画面
- 担任ホーム
- 出席・健康
- 心の天気ダッシュボード
- 月間出席簿
- 学期・年度集計
- 学校暦
- 学期設定
- 初期設定
- 実装プレビュー
- 横はみ出し
- nowrap文字切れ
- JavaScriptエラー
- タスク画像読込
- 画像・文字・チェックマークの重なり

## 完成判定までの残工程

1. GitHub Actionsの最新ジョブが実行されることを確認
2. Smoke / Visual Checkの結果を確認
3. 問題があれば該当箇所だけ局所修正
4. 生成スクリーンショットの最終目視
5. README / LAN_SETUP / QA_CHECKLIST / ATTENDANCE_DESIGN / PR本文の整合確認
6. 実機でしか確認できない項目を導入前確認として明確に切り分け
7. CI成功後にDraft解除・マージ判断

## 学校実機で最終確認する項目

- Windows担任PCでの `npm install` / `npm run preflight` / `npm start`
- Windows Defender Firewall越しの接続
- iPad / Androidブラウザからの接続
- 複数タブレットへのリアルタイム配信
- WebSocket切断 / 復帰
- 学校MDM方針
- C4th実CSVの列対応

実機確認が必要な項目は、コード未完成と混同しない。

## 完成工程の原則

- 新機能をむやみに増やさない
- 正常な画面・ロジックには触れない
- 修正は局所的に行う
- 退避状態を維持する
- CI成功前にDraft解除・マージをしない
- 現行版完成後に、リポジトリ構成整理などの保守作業を別工程で行う

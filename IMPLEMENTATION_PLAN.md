# 朝のチェックイン 実装計画・完成状態

更新日: 2026-08-21

## この文書の位置づけ

この文書は当初のPhase 1〜7実装計画として作成した。現在は主要実装と開発環境での自動確認・画面確認まで完了しているため、現行版の完成状態と導入前確認を記録する文書として扱う。

今後、新機能案と学校実機でしか確認できない項目を「コード未完成」と混同しない。

## 目的

校内LANで運用するWebアプリとして、児童の朝のチェックイン、心の天気、朝の準備タイマー、出席・健康観察、月間・学期・年度集計を一体化する。

忙しい担任が毎朝迷わず使えることを最優先とし、児童側はiPad / Android等のブラウザ利用を基本とする。

## 現在実装済みの主機能

- 児童用の朝チェックイン画面
- 低学年向け表示 / 高学年向け表示
- 学年・組表示
- 現在時刻、朝の会までの残時間、タスクタイマー
- 心の天気4段階
- 朝のチェック項目、自由追加、完了操作
- 正式タスクSVG5種と画像読込失敗時のフォールバック
- 担任ホーム
- 学級・端末・画面初期設定
- C4th CSV名簿取込の受け皿
- 担任用タスク編集・プリセット・学年別プレビュー
- Node.js / Express / WebSocket校内LANサーバー
- 端末ACK、接続状況、再接続、オフライン継続
- 今日の出席・健康観察
- 「未チェックイン＝欠席確定」にしない未確認運用
- 日次確定と監査履歴
- 月間出席簿の横計・縦計・理由別集計
- 学期・年度集計
- 授業日カレンダー、振替休業等の除外
- 学期設定
- 心の天気の週間・月間分析と行事メモ
- Smoke Test / Visual Check / preflight

## 壊さないためのデータ原則

出席・健康観察・心の天気は、同じ担任画面で参照する場合があっても別データとして扱う。

- 心の天気を出欠区分へ自動変換しない
- 心の天気を健康状態へ自動変換しない
- 出席・健康の保存で心の天気を上書きしない
- 未チェックインを即欠席にしない
- 正式な欠席理由・遅刻・早退は担任が確定する

## 端末運用方針

標準はインストール不要のブラウザ運用。

- iPad: Safari等から校内LAN URLを開く
- Android: Chrome等から校内LAN URLを開く
- PWA / Web Clip / 自動起動: 学校MDMで許可される場合のみ
- Webページ単独ではブラウザの指定時刻強制起動はできない
- 一時切断時は直前設定で継続し、復帰後に自動再接続

詳細は `git clone/school-checkin-timer/LAN_SETUP.md` を正とする。

## 現在のアプリ配置について

現行版ではアプリ本体が次のパスにある。

```text
git clone/school-checkin-timer/
```

このネストは一般的なリポジトリ構成としては不自然だが、現在のGitHub Actions、相対パス、プレビュー、運用文書がこの配置を前提に正常動作している。

**現行完成版では移動しない。**

リポジトリ構成の整理は、現行版を固定した後の独立した保守作業として扱う。実施する場合は別ブランチで全パス・CI・静的資産・起動手順を一括検証する。

## 自動確認

### `npm run preflight`

- Node.js 20以上
- 主要画面・主要ファイル
- 正式タスクSVG5種
- データ保存ディレクトリ書込み
- npm scripts
- 8080番ポート利用可否

### `npm run test:smoke`

- サーバーAPI
- WebSocket
- 設定同期
- 出席・健康・心の天気の分離
- 学校暦・学期設定
- 主要HTML
- 正式SVG取得

### `npm run test:visual`

35パターンを対象に、1366 / 1024 / 768 / 375pxを中心として以下を確認する。

- 低学年 / 高学年児童画面
- 担任ホーム
- 出席・健康
- 心の天気ダッシュボード
- 月間出席簿
- 学期・年度集計
- 学校暦
- 学期設定
- 初期設定
- 実装プレビュー
- 横はみ出し
- nowrap文字切れ
- JavaScriptエラー
- タスク画像読込
- 画像・文字・チェックマークの重なり

## 開発版の完成確認

2026-08-21の完成確認では、GitHub Actionsで以下が成功している。

- preflight
- LAN server起動・health確認
- Smoke Test
- Responsive Visual Check 35パターン
- Visual screenshots artifact生成

生成された35枚についても主要画面を目視確認し、1366 / 1024 / 768 / 375pxで新たな操作不能、横はみ出し、実要素重なりは確認していない。

PR #1で主要実装を `main` へ統合し、PR #2で `main` push時にもCIが走るようにした。PR #3では完成状態の文書を現状へ合わせて更新した。

したがって、現時点の残件は「開発版の基本機能をさらに増やすこと」ではなく、学校実機での導入確認と、将来拡張を必要時に別工程で扱うこととする。

## 学校実機で最終確認する項目

- Windows担任PCでの `npm install` / `npm run preflight` / `npm start`
- Windows Defender Firewall越しの接続
- iPad / Androidブラウザからの接続
- 複数タブレットへのリアルタイム配信
- WebSocket切断 / 復帰
- 学校MDM方針
- C4th実CSVの列対応

実機確認が必要な項目は、コード未完成と混同しない。

## 将来拡張として別工程に残すもの

- C4thへ戻すための標準CSV出力の仕上げ
- C4th正式取込テンプレート入手後の専用形式出力
- UTF-8 BOM / Shift-JIS 出力切替
- 学校様式に合わせたCSV / Excel / PDF帳票出力
- PWA / MDM / Web Clip等の高度な端末配布・起動支援
- 現在のネストしたリポジトリ配置の整理

## 完成版保守の原則

- 新機能をむやみに現行完成版へ追加しない
- 正常な画面・ロジックには不用意に触れない
- 修正は局所的に行う
- 出席・健康・心の天気のデータ分離を維持する
- 変更時はpreflight / Smoke / Visual Checkを再実行する
- 学校実機依存の問題とコード不具合を切り分ける
- 大きな構成変更は別ブランチ・別工程で行う

# Local class data

このフォルダ配下の `classes/` は、校内LANサーバーが学級ごとの「今日の朝の設定」を保存する場所です。

- 外部クラウドへ送信しません。
- `server/server.js` 起動時に `classes/` を自動作成します。
- `1-1.json` のように学級IDごとに保存します。
- 心の天気など児童個人データは、このPhase 4ではまだ保存しません。

運用時は学校の情報管理方針に従ってバックアップ・保存期間を決めてください。
